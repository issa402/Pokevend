global:
  scrape_interval: 30s
  evaluation_interval: 30s

scrape_configs:
  - job_name: pokemon-server
    metrics_path: /metrics
    static_configs:
      - targets:
          - server:3001 # Containers use service names. Laptop uses localhost:3001; Prometheus uses server:3001.
